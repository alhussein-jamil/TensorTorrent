# Baseline metrics (2026-07-29)

CPU-only VM. No GPU. Virtual accelerators = **simulated**.

## Environment

- `torch 2.13.0+cu130` (CPU execution)
- `maturin develop --release`
- branch `main` after NativeCompiledArtifact + region data-plane path

## Suite results

| Suite | Result |
|-------|--------|
| `cargo test --workspace --exclude streamcompiler-python` | pass |
| `pytest -q` | **316 passed** |
| Criterion `empty_schedule` | ~93 ns |
| Criterion `linear_64_dry_run` | ~62 µs (~0.97 µs/op) |

## Public path classification

| Path | Evidence |
|------|----------|
| Native scheduler | `native_runtime=True`, `scheduler_enters` |
| Persistent artifact | `native_artifact_reused=True`, hot-path `schedule_from_py=0` |
| Native data plane (resident CPU) | `native_data_plane=True`, **1** GIL callback/forward (Compute) |
| Streaming / transfer / mock | instruction-callback fallback (Python tensor bodies) |
| Python CopyStore | still holds torch values |
| Native simulator | opt-in `STREAMCOMPILER_NATIVE_SIM=1` |
| Python fallback DAG | `STREAMCOMPILER_ALLOW_PYTHON_RUNTIME=1` only |

## Latency (Linear 8→4)

| Metric | Session start | Now |
|--------|---------------|-----|
| Schedule convert / forward | yes | **0** |
| Callbacks / forward | 2 | **1** |
| Dispatch overhead vs eager | ~1600–2000 µs | **~260 µs** |
| Micro-dispatch `<500 µs` | fail | **pass** |

## Remaining (priority)

1. Opaque tensor handles — Rust residency authoritative for torch values
2. Native Transfer / streaming Load / spill
3. Async virtual backend
4. Simulator default on
5. Native storage + profiler wiring
6. Delete Python DAG default path after parity
